import { Typography } from "@mui/material";

function Header() {
  return (
    <header id="header">
      <Typography variant="h1" component="h1" sx={{ color: "#004643" }}>
        NC News
      </Typography>
    </header>
  );
}

export default Header;
